<menu xmlns:android="http://schemas.android.com/apk/res/android">
    
    <item
		android:showAsAction="always"
        android:id="@+id/clear"
        android:title="清空"/>
    <item
		android:showAsAction="always"
        android:id="@+id/save"
        android:title="保存"/>
</menu>
